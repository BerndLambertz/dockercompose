input {
  stdin { }
  http {
  }
  tcp {
    port => 514
    type => syslog
  }
  udp {
    port => 514
    type => syslog
  }
}

output {
  elasticsearch { host => localhost }
  stdout { codec => rubydebug }
}
