input {
  stdin { }
  http {
    port => ["80", "8080", "5601", "9200"]
  }
  http {
    port => 80
  }
  tcp {
    port => 514
    type => syslog
  }
  udp {
    port => 514
    type => syslog
  }
}

output {
  elasticsearch { host => localhost }
  stdout { codec => rubydebug }
}
